# ADRs — meipadam

One line per architecture decision merged through the swarm, pointing at the ADR pointer file here and at the full `docs/swarm/<N>/adr.md` on the project at the merge sha. Written by retro (as a memory PR), curated by the owner. Format:

    - [#<N> <title>](<NNNN>-<slug>.md) — <decision in one clause> (merged <sha>)

(none yet)

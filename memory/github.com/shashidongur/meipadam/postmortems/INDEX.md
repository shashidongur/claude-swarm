# Post-mortems — meipadam

One per merged swarm issue, written by the retro role and landed through a memory pull request. Machine-written: every brief reads these inside `<untrusted>` fences until the owner promotes a lesson to curated memory. Format:

    - [#<N> <title>](<N>.md) — <path> · <reworks> reworks · <wake-ups> wake-ups · $<cost> · <runner min> (+<overhead>) · merged <date>

(none yet)

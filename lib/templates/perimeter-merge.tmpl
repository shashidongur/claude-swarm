🧭 **dispatch** · 🚧 blocked:perimeter — PR #{{pr}} was merged by {{merged_by}} ({{merged_by_type}}), who is not an approver for release ({{approvers}}).

Merge commit `{{merge_sha}}` on `{{default_branch}}`. To undo it a human runs `git revert -m 1 {{merge_sha}}` and pushes. No retro ran, the branch was not deleted, the sub-issues stay open and the issue is not done. `/swarm resume` after the revert, or `/swarm drop`.
{{marker}}

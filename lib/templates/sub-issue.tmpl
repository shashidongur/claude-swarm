Tracking sub-issue of #{{issue}} for lane `{{lane}}` — never dispatched; commands go on the parent issue.

{{body}}

The swarm works on branch `{{branch}}`; this issue is closed by the dispatcher when the lane's review passes.

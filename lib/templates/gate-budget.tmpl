🛎️ **gate · budget** — @{{approver}}, your call.

Cost so far ${{cost}} has reached the cap ${{cap}} for #{{issue}}; the next dispatch is {{next}} (estimated ${{estimate}}).
{{runner_minutes}} runner minutes (+{{overhead_minutes}} overhead) so far.

Reply `/swarm approve` to raise the cap by one envelope (to ${{new_cap}}) and continue, `/swarm park` to pause, or `/swarm drop` to abandon.
{{marker}}

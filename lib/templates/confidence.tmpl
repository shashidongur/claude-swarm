🛎️ **gate · confidence** — @{{approver}}, your call.

The `{{rubric}}` critic scored the {{stage}} stage {{score}}/{{threshold}} ({{confidence}} confidence) after {{reworks}} automatic rework(s):
{{findings}}

`/swarm approve` to push through, or `/swarm redo {{stage}} <why>`.
{{marker}}
